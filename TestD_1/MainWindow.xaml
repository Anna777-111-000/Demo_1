<Window x:Class="TestD_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestD_1"
        mc:Ignorable="d"
 Title="Партнеры компании" 
        Height="550" Width="900">

    <Window.Resources>
        <DataTemplate x:Key="PartnerCard">
            <Border Background="{StaticResource SecondaryBackground}" 
                    BorderBrush="Black" 
                    BorderThickness="1" 
                    Margin="20,0,20,10" 
                    Padding="15,12"
                    MouseLeftButtonDown="Card_Click"
                    Cursor="Hand">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding PartnerName}" FontWeight="Bold"/>
                            <TextBlock Text=" | " FontWeight="Bold"/>
                            <TextBlock Text="{Binding PartnerType}" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Director}"/>
                        <TextBlock Text="{Binding Phone}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Рейтинг: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding Reiting}"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding Discount}"
                               FontSize="26" FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <Image Source="/Images/logo.png" Width="40" Height="40" Margin="0,0,15,0" Stretch="Uniform"/>
            <TextBlock Text="Партнеры компании" FontSize="24" FontWeight="Bold"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Партнер" FontWeight="Bold"/>
            <TextBlock Grid.Column="1" Text="Скидка" HorizontalAlignment="Right" FontWeight="Bold"/>
        </Grid>

        <Separator Grid.Row="2" Background="Black" Height="1" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="cardsControl" ItemTemplate="{StaticResource PartnerCard}"/>
        </ScrollViewer>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Height="48" VerticalAlignment="Top">
            <Button x:Name="btnAdd" Content="Добавить" 
                    Width="100" Margin="0,15,0,0"
                    Click="AddPartner_Click"/>
        </StackPanel>
    </Grid>
</Window>
